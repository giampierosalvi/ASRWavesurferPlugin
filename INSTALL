Linux:
- install julius with
sudo apt-get install julius
- install wavesurfer (if you do not have it already) with
sudo apt-get install wavesurfer
- copy asr.plug into ~/wavesurfer/<version>/plugin/
- copy the models files to ~/wavesurfer/<version>/plugin/asr

Windows:

Mac OSX:
